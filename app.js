Vue.createApp({
    data() {
        //gets called when created
        return {
            firstName: 'Pedro'
        }
    }
}).mount('#app')
//what element it controls
//expressions: evaluates to a single value, 